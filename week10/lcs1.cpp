#include <iostream>

using namespace std;

#define fastio                   \
    ios::sync_with_stdio(false); \
    cin.tie(NULL);               \
    cout.tie(NULL);

int dp[201][201];

int main()
{
    fastio;
    string s,t;
    cin >> s >> t;
    return 0;
}